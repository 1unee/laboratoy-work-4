spring:
  application.name: laboratory-work
  datasource:
    url: jdbc:h2:mem:user
    driver-class-name: org.h2.Driver
    username: username
    password: password
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
  sql.init.mode: always
  flyway.enabled: true

server.api.version.users: v1
